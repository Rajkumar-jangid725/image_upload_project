import React from 'react';

const ViewImageButton = ({ image }) => {
  const handleViewImage = () => {
    // Logic to view the full original image
  };

  return (
    <button onClick={handleViewImage}>
      View Image
    </button>
  );
};

export default ViewImageButton;
